//7.Escribir un programa en JavaScript que use una estructura "if-else" para determinar si un número es divisible por 3 o no.
let numero = 16;

if (numero % 3 === 0) {
    console.log(`${numero} es divisible por 3`)
} else {
    console.log(`${numero} No es divisible por 3`)
}